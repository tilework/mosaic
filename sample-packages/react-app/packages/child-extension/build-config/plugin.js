module.exports = {
    plugin: {
        overrideWebpackConfig: (config) => {
            return config;
        },
        overrideBabelConfig: (config) => {
            return config;
        }
    }
};
